repo:
  - name: version2
    ask:
      - parameter:
          name: myparameter
          defaultValue: This is my default value!
          usage: For tests purposes
    hooks:
      version: |
        echo ${myparameter}